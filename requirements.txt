# ComfyUI-AudioX requirements
# Target: Python 3.12, PyTorch cu128
#
# torch / torchvision / torchaudio are NOT listed here because ComfyUI
# already installs a specific CUDA build. Re-installing them via pip
# would downgrade or break the existing build.
#
# k-diffusion is NOT listed because ComfyUI typically ships it, and
# re-installing it pulls in clip-anytorch which downgrades numpy.
# If k-diffusion is missing, install it manually:
#   pip install k-diffusion==0.1.1.post1
#   pip install "numpy>=2.0.0"   # restore after clip-anytorch downgrades it

# Core
einops>=0.7.0
safetensors>=0.4.0
soundfile>=0.12.1

# Video decoding
decord>=0.6.0

# CLIP conditioner
transformers>=4.40.0

# Synchformer (AudioX-MAF / AudioX-MAF-MMDiT only)
timm>=0.9.0
omegaconf>=2.3.0

# Audio codec and quantization
descript-audio-codec>=1.0.0
einops-exts>=0.0.4
vector_quantize_pytorch>=1.0.0